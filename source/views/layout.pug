section.container.layout#default-layout
  .row#calculator-wrapper
    .twelve.columns
      .row.text-center#aspect-ratio-buttons-wrapper
        mixin buttons(...items)
          each item in items
            .three.columns.aspect-ratio-button
              button.u-full-width= item
        +buttons('16 : 9', '21 : 9', '4 : 3', '2 : 1')
      .row#values-wrapper
        .one-half.column#original-values
          .input-wrapper
            label(for='w1') w1
            input(type='text',name='w1',class='u-full-width',id='w1',placeholder='1920')
          .input-wrapper
            label(for='h1') h1
            input(type='text',name='h1',class='u-full-width',id='h1',placeholder='1080')
        .one-half.column#new-values
          .input-wrapper
            label(for='w2') w2
            input(type='text',name='w2',class='u-full-width',id='w2')
          .input-wrapper
            label(for='h2') h2
            input(type='text',name='h2',class='u-full-width',id='h2')
      .row
        .eleven.columns#aspect-ratio-wrapper
          | Current aspect ratio: #[strong#aspect-ratio-suggestion]
        .one.column#reset-button
          span.fas.fa-sync
  .row#info-wrapper
    .twelve.columns
      h2=project_name
        span#version=project_version
        span.subtitle=project_oneliner
      p While resizing images, use this calculator to preserve their aspect ratio. The formula used by this app is:
      p
        em (h1 / w1) x w2 = h2
      p Where h1 is the original height, w1 is the original width, w2 is the new width and h2 is the new height.
  footer.row.text-center#copy
    .twelve.columns
      .row
        .twelve.columns
          span.fas.fa-ellipsis-h#info-button
      a(href=project_url target='_blank') &lt;code&gt;
      | &nbsp;by&nbsp;
      a(href=author_url target='_blank')=author_name